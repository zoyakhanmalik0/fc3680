# Replaceable Images

Swap these files with your own pictures and keep the same filenames:

- `app-icon.svg` - app icon at the top
- `screenshot-1.svg` - first game screenshot
- `ad-banner.svg` - second ad/banner image

You can also use `.png` or `.jpg`; just update the matching `src` in `index.html`.
